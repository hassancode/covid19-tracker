import React, { useState, useEffect } from 'react';

const Chart = () => {
    const [dailyData, setDailyData] = useState({});

    // useEffect(async () => {
    //     setDailyData(api.fetchDailyData());
    // });
    return (
        <h1>Chart</h1>
    )
}
 
export default Chart;